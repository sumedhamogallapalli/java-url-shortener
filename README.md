# Java URL Shortener

## 📌 Project Description
This project is a Java-based URL Shortener application that converts long URLs into unique short links and retrieves the original URLs using a MySQL database. It demonstrates core Java concepts along with JDBC and database connectivity.

## 🛠 Technologies Used
- Java
- JDBC
- MySQL
- VS Code

## ✨ Features
- Generates unique short URLs for long links
- Stores URL mappings in MySQL database
- Retrieves original URLs using short codes
- Menu-driven console-based application
- Uses PreparedStatement for secure database operations

## ▶ How to Run the Project
1. Install Java (JDK)
2. Install MySQL and create a database named `urlshortener`
3. Create table `urls` with required fields
4. Download MySQL Connector/J (JDBC driver)
5. Compile the project:
6. Run the application:

## 👤 Author
Sumedha Mogallapalli

